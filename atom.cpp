#include "atom.h"

Obiekt2D::Obiekt2D (int kolor, double masa, int promien, double x0, double y0, double vx0, double vy0) :
	k (kolor), m (masa), r (promien), s (x0, y0), v (vx0, vy0)
{
	// celowo on jest pusty i tak jest fajnie  
}
